name: weather-doc-extractor
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pillow
  - matplotlib
  - nbformat
  - pip:
      # Pin torch to a CUDA 12.1 wheel explicitly. Without this, pip resolves
      # the torch dependency from accelerate/trl/peft and upgrades to the
      # latest wheel (currently CUDA 12.6+), which breaks on V100 nodes whose
      # driver only supports up to CUDA 12.4.
      - --extra-index-url https://download.pytorch.org/whl/cu121
      - torch==2.4.0+cu121
      - torchvision==0.19.0+cu121
      - accelerate
      - datasets
      - huggingface_hub
      - num2words
      - peft
      # transformers: >=5.8.0 required for native Granite 4.1 support (IBM tested).
      # Gemma4Processor also requires 5.x; Gemma 3 requires >=4.50.0.
      - transformers>=5.8.0
      - trl
      - scipy
